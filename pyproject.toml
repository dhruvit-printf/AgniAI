[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"

[tool.black]
exclude = '''
(
  /(
      \.eggs
    | \.git
    | \.hg
    | \.mypy_cache
    | \.nox
    | \.tox
    | \.venv
    | agniai-env
    | agniai-env-broken
    | _build
    | buck-out
    | build
    | dist
  )/
)
'''

[tool.isort]
profile = "black"
skip = ["agniai-env", "agniai-env-broken", ".venv", "build", "dist"]
