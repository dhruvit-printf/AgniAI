# AgniAI dependencies
# Requires Python 3.9+
# Install with:  pip install -r requirements.txt

# Web framework + CORS (for .NET and React integration)
flask>=3.0.0
flask-cors>=4.0.0

# Vector search
faiss-cpu>=1.7.4

# Embeddings (local, offline after first download)
sentence-transformers>=2.7.0

# PDF extraction
PyMuPDF>=1.23.0

# Word document extraction
python-docx>=1.1.0

# Web scraping / HTTP
requests>=2.31.0
beautifulsoup4>=4.12.0

# Numerics
numpy>=1.24.0

# Hybrid retrieval (BM25 keyword search — used when USE_HYBRID=1)
rank-bm25>=0.2.2

# System monitoring (used by benchmark_test.py)
psutil>=5.9.0