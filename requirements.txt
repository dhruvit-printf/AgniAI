# AgniAI dependencies
# Requires Python 3.11 (recommended) | Python 3.10–3.12 supported
# Install with:  pip install -r requirements.txt

# Web framework + CORS (for .NET and React integration)
flask>=3.1.3
flask-cors>=6.0.5
flask-limiter>=4.1.1

# Vector search
faiss-cpu>=1.14.3

# Embeddings (local, offline after first download)
sentence-transformers>=5.6.0

# PDF extraction
PyMuPDF>=1.27.2.3

# Word document extraction
python-docx>=1.2.0

# Web scraping / HTTP
requests>=2.34.2
beautifulsoup4>=4.15.0

# Numerics
numpy>=2.2.6

# Hybrid retrieval (BM25 keyword search — used when USE_HYBRID=1)
rank-bm25>=0.2.2

# System monitoring (used by benchmark_test.py)
psutil>=7.2.2

python-dotenv>=1.2.2

pydantic-settings>=2.14.2